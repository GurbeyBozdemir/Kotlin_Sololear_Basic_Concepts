```Kotlin
/**
 * 3-Variables
 * Değişkenler var keywordu ile tanımlanır.
 */
var num: Int = 42

var name: String = "James"

//val keywordu ile de tanımlanabilir ancak val ile tanımlanan değişken daha sonra değiştirilemez.
val course: String = "Kotlin"
val color: Int = 42

// Kotlin değişken türünü kendisi algılayabilir. Bu durumda değişken aşağıdaki şekilde tanımlanabilir.
val name = "James"
var num = 42
// test değişkenin türü nedir?
val test = "88"
// cevap: String
```
