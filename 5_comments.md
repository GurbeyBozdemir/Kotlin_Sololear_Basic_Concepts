```Kotlin
/**
 * 5-Comments
 * Yorumlar koda açıklama eklemek için kullanılır.
 * Birden çok satır halinde yorum eklemek için "/*" ve "*/" ifadelerinin arası kullanılır. Şuan buna örnek bir yorum okuyorsunuz.
 * Tek satır içerisinde yorum ise "//" ifadesi kullanılarak eklenir.
 */
// my first Kotlin program
fun main (args: Array<String>) {
    // declaring a new variable
    var name = "Amy"
    println(name)
}

var x = 8
var y = x*2 // doubles the value
println(y)

/*
 "Kotlin is fun"
*/
```
